import { Sortbynamea2Pipe } from './sortbynamea2.pipe';

describe('Sortbynamea2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Sortbynamea2Pipe();
    expect(pipe).toBeTruthy();
  });
});
