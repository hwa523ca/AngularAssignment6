import { SortbynameaPipe } from './sortbynamea.pipe';

describe('SortbynameaPipe', () => {
  it('create an instance', () => {
    const pipe = new SortbynameaPipe();
    expect(pipe).toBeTruthy();
  });
});
