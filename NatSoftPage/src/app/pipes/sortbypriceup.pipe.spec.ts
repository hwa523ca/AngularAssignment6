import { SortbypriceupPipe } from './sortbypriceup.pipe';

describe('SortbypriceupPipe', () => {
  it('create an instance', () => {
    const pipe = new SortbypriceupPipe();
    expect(pipe).toBeTruthy();
  });
});
