import { Sortbynamez2Pipe } from './sortbynamez2.pipe';

describe('Sortbynamez2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Sortbynamez2Pipe();
    expect(pipe).toBeTruthy();
  });
});
