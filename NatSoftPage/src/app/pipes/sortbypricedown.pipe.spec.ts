import { SortbypricedownPipe } from './sortbypricedown.pipe';

describe('SortbypricedownPipe', () => {
  it('create an instance', () => {
    const pipe = new SortbypricedownPipe();
    expect(pipe).toBeTruthy();
  });
});
